using UnityEngine;

public class ObfuscatorBuildGuard : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        this.Log("");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
